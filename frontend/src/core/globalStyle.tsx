import { css } from "@emotion/core";

export const Contents = css({
  padding: 8,
  overflowX: "scroll"
});

export const Header = css({
  gridArea: "Header",
  display: "flex",
  background: "#fff",
  alignItems: "center",
  padding: 8,
  zIndex: 1
});
